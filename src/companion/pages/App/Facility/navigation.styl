.nav a span.fa {
     margin-right: 0.5em;
}

.navbar-default .facility-dropdown button
   &:hover 
       color: #fff;
       text-decoration: none;
   &:focus 
       color: #fff;
       text-decoration: none;
